// This application uses AT Protocol for data storage
// Server-side storage is not needed as all data is stored via Bluesky/AT Protocol

export interface IStorage {
  // Placeholder interface for future server-side features if needed
}
